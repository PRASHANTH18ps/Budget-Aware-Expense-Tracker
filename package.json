{
  "name": "budget-aware-expense-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "npm install --prefix Backend && npm install --prefix Frontend",
    "dev": "npm run bootstrap && concurrently -k \"npm run dev --prefix Backend\" \"npm start --prefix Frontend\"",
    "start:backend": "npm start --prefix Backend",
    "start:frontend": "npm start --prefix Frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "react-toastify": "^11.0.5",
    
  }
}
